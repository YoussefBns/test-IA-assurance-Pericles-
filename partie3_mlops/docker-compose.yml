services:
  churn-api:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - churn-logs:/app/logs
    restart: unless-stopped
volumes:
  churn-logs:
